<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.mvc.Interface.IBrandDAO">


    <select id="getBrands" resultType="com.team.mvc.DTO.BrandDTO">
        SELECT BRAND_ID as brandId,
               BRAND_NAME as brandName
        FROM BRAND
        ORDER BY BRAND_ID
    </select>

    <!-- 브랜드 이름으로 브랜드 정보 조회 -->
    <select id="getBrandByName" resultType="com.team.mvc.DTO.BrandDTO">
        SELECT BRAND_ID as brandId,
               BRAND_NAME as brandName
        FROM BRAND
        WHERE BRAND_NAME = <![CDATA[ #{brandName} ]]>
    </select>

    <!-- 브랜드 ID로 브랜드 정보 조회 -->
    <select id="getBrandById" resultType="com.team.mvc.DTO.BrandDTO">
        SELECT BRAND_ID as brandId,
               BRAND_NAME as brandName
        FROM BRAND
        WHERE BRAND_ID = #{brandId}
    </select>
</mapper>